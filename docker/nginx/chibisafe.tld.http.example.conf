server {
    listen              80;
    listen              [::]:80;
    server_name         chibisafe.tld;

    # security
    include             nginxconfig.io/security.conf;

    # logging
    access_log          /var/log/nginx/chibisafe.tld.access.log;
    error_log           /var/log/nginx/chibisafe.tld.error.log warn;

    # uploaded files
    location / {
        add_header Access-Control-Allow-Origin *;
        root /usr/chibisafe/uploads;
        try_files $uri @proxy;
    }

    # reverse proxy
    location @proxy {
        proxy_pass http://chibisafe:9999;
        include    nginxconfig.io/proxy.conf;
    }

    # additional config
    include nginxconfig.io/general.conf;
}
